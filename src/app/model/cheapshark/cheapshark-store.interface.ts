export interface CheapSharkStoreInterface {
    images?: {
        banner?: string,
        icon?: string,
        logo?: string
    },
    isActive: boolean,
    storeID: string,
    storeName: string
}